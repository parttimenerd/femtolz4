# jqwik configuration — read at test startup from classpath
# Override per-property with @Property(tries=N)
# In deep-fuzz mode this file is replaced by the filtered version with a higher count.
jqwik.tries.default=${fuzz.tries.default}
