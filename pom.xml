<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>me.bechberger</groupId>
    <artifactId>femtolz4</artifactId>
    <version>0.1.0</version>
    <packaging>jar</packaging>

    <name>femtolz4</name>
    <description>Minimal pure-Java LZ4 frame-format compressor and decompressor. No native libs, no dependencies.</description>
    <url>https://github.com/parttimenerd/femtolz4</url>

    <licenses>
        <license>
            <name>MIT</name>
            <url>https://opensource.org/licenses/MIT</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>parttimenerd</id>
            <name>Johannes Bechberger</name>
            <email>me@mostlynerdless.de</email>
        </developer>
    </developers>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- set -Dnative.skip=true to skip the build_native.py step -->
        <native.skip>false</native.skip>
        <!--
          Test suite selection (JUnit Platform tag filtering; applies to both
          JUnit Jupiter @Test and jqwik @Property, since jqwik's @Tag maps to
          the same platform tags):
            mvn test                    -> fast suite (excludes "slow")
            mvn test -Dtest.full=true   -> full suite (nothing excluded)
        -->
        <test.full>false</test.full>
        <test.excludedGroups>slow</test.excludedGroups>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>at.yawk.lz4</groupId>
            <artifactId>lz4-java</artifactId>
            <version>1.11.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>net.jqwik</groupId>
            <artifactId>jqwik</artifactId>
            <version>1.8.5</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>17</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.3.0</version>
                <executions>
                    <execution>
                        <id>build-native</id>
                        <!-- runs before resources are copied into the JAR -->
                        <phase>generate-resources</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <executable>python3</executable>
                            <arguments>
                                <argument>${project.basedir}/build_native.py</argument>
                            </arguments>
                            <!-- skip with -Dnative.skip=true -->
                            <skip>${native.skip}</skip>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.2</version>
                <configuration>
                    <excludedGroups>${test.excludedGroups}</excludedGroups>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>me.bechberger.femtolz4.Main</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Activated by -Djava.only=true: pure-Java JAR, no bundled native libs.
             Output: target/femtolz4-java-0.1.0.jar -->
        <profile>
            <id>java-only</id>
            <activation>
                <property><name>java.only</name><value>true</value></property>
            </activation>
            <properties>
                <native.skip>true</native.skip>
            </properties>
            <build>
                <finalName>femtolz4-java-${project.version}</finalName>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                        <version>3.3.0</version>
                        <configuration>
                            <excludes>
                                <exclude>native/**</exclude>
                            </excludes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!-- Activated by -Dtest.full=true: runs the full test suite, including
             tests tagged "slow" (exhaustive truncation sweeps, 100MB fuzz, etc). -->
        <profile>
            <id>full-tests</id>
            <activation>
                <property><name>test.full</name><value>true</value></property>
            </activation>
            <properties>
                <test.excludedGroups></test.excludedGroups>
            </properties>
        </profile>
    </profiles>
</project>
